/* aff_a */

#include <unistd.h>

int main(void)
{
    write(1, "a", 1);
    write(1, "\n", 1);
    return (0);
}

/* aff_first_param */

#include <unistd.h>

int main(int argc, char \*\*argv)
{
    if (argc > 1)
        write(1, argv[1], sizeof(argv[1]));
    write(1, "\n", 1);
    return (0);
}
