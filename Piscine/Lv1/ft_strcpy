/* ft_strcpy */

char *ft_strcpy(char *dest, char *src)
{
    int i = 0;

    while (src[i])
    {
        dest[i] = src[i];
        i++;
    }
    dest[i] = '\0'; // Null-terminate the copied string
    return (dest);
}